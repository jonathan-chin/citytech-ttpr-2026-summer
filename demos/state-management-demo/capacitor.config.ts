import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.citytech.statemanagementdemo',
  appName: 'state-management-demo',
  webDir: 'dist'
};

export default config;
